server:
  port: 8081

spring:
  application:
    name: user-service
  profiles:
    active: local
    include: swagger
  data:
        mongodb:
            uri: mongodb://localhost:27017
            database: emv_ms_users
    
